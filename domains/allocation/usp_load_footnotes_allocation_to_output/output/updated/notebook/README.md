# Databricks benchmark notebook

This directory contains only the benchmark notebook. Importable Python modules
remain one level up in `output/updated/`.

Sync the whole `output/updated` directory to the monolith's matching
`.../usp_load_footnotes_allocation_to_output/output/updated` destination.
