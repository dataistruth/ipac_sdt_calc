# Platform Framework — migrate from Source/Framework

Contains orchestrator integration, payloads, pool/policy scripts.

Shell scripts from monolith (reference paths after migrate):
- `recreate_pool_and_policy.sh`
- `test_one_time_run.sh`
- `test_parallel_runs_in_pool.sh`
- `payloads/`

Deploy pool/policy resources via ipac-sdt-calc bundle when added to `resources/`.
